for (let i = 0; i < 10; i++) {
    console.log(i)
}

console.log('i =', i)

/**
* Para reforçar, nesse exemplo foi usado uma estrutura de repetição e declarado uma variável com (let), mas como já vimos, 
* variaveis declaradas como (let) tem o escopo de bloco de código, então quando dentro de uma estrutura de repetição, 
* essa variável não é visivel/acessivel em outro lugar do sistema, a não ser dentro da própria estrutura.
*/